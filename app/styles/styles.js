import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  // tus estilos aquí
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 100,
    height: 100,
  },
  // etc.
});

export default styles; // 👈 ¡esto es obligatorio!
